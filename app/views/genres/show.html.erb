<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<p>
  <strong>Ime:</strong>
  <%= @genre.name %>
</p>

<%= link_to 'Uredi', edit_genre_path(@genre), class: 'add-button' %>
<%= link_to 'Nazaj', genres_path, class: 'add-button' %>
